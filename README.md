# NER v2 — push all 20 files to repo root (CNAME: newarkeliteroofing.com)
- style.css: call bar shortened to 44px (was 60px). Phone number now shows in the header at every width, beside the menu button on phones and tablets. Long call buttons no longer overflow small phones.
- 19 HTML pages: the duplicate phone number next to "Free Inspections — No Obligation" in the top bar is removed. The number now appears once, in the header.
Push the HTML and style.css together.
